`Objectif final` :

Un pipeline Airflow dans Docker qui télécharge chaque jour un fichier de données publiques de transport, le nettoie en Python et le charge dans PostgreSQL. Le tout versionné sur GitHub.